# Flutter 教程项目规范

## 项目目标

本项目用于制作一套以 iOS 为主平台，同时支持 Android 与 HarmonyOS NEXT／OpenHarmony 的中文 Flutter 教程。

教程必须帮助读者完成可运行、可验证的应用，不能写成 API 罗列或无法复现的概念说明。

## 内容边界

- 默认读者位于中国大陆，使用中文操作系统和大陆常见网络环境；海外环境作为差异说明，不作为默认路径。
- iOS 是主讲平台、主截图平台和逐章验证平台。
- Android 是 Flutter 官方支持的副平台，只说明相对 iOS 的工具链、行为与发布差异，不进行运行、构建或截图验证。
- 鸿蒙指 HarmonyOS NEXT／OpenHarmony，使用 Flutter-OH 适配，不得描述成 Flutter 官方支持平台。
- iOS、Android 使用 Flutter 上游稳定版；鸿蒙使用 Flutter-OH 稳定版。
- 不覆盖 Flutter Web、桌面端、游戏开发和大型企业架构。

## 文档规范

- 所有教程和项目文档使用中文；代码、命令、变量名和文件名使用英文。
- 结论先行，一篇只解决一个中心问题。
- 抽象概念按「场景 → 解释 → 代码 → 验证」展开。
- 每篇必须包含学习目标、动手步骤、预期结果、常见问题和完成检查。
- 涉及版本、命令、平台能力和发布规则时，必须引用官方或项目一手来源。
- 面向读者的教程只写已经完成一手来源核验、且按文中步骤可以执行的内容；iOS 代码和构建步骤还必须实际运行通过。Android 内容是例外：完成官方一手来源核验后可以说明相对 iOS 的差异，但必须明确未做实际运行或构建验证，不得写成已经通过。
- 未核验的版本、插件、服务、命令、兼容性和发布要求不得以“待确认”“可能支持”或占位表形式出现在教程正文中。
- 暂时无法验证的内容从教程中移除，只记录在 `ROADMAP.md` 的待办或阻塞中，验证完成后再加入正文。
- 易变化内容必须标注最后核验日期，避免使用没有版本语境的“最新”。
- 涉及 GitHub、Google 托管资源、`pub.dev` 等大陆网络可能不稳定的服务时，必须说明网络前提、官方文档列出的可信镜像或可验证替代路径。
- 不默认读者拥有国际网络、境外手机号、境外支付方式、Google Play 服务或海外开发者账号。
- 涉及应用商店、地图、推送、登录、支付、统计和云服务时，先给中国大陆可执行路径，再补充海外差异。
- 网络问题必须区分下载源、包镜像、代理、账号区域和服务本身不可用，不能统一归因于“需要魔法”。
- 镜像、第三方服务和国内替代方案必须标注维护方、适用范围和核验日期，不推荐来源不明的下载站。
- 不虚构个人经历；踩坑内容必须可复现，或明确注明来源。
- 内部链接使用相对路径，外部链接直接指向支持相关结论的页面。

## 代码规范

- 示例代码以最小可运行实现为准，不为教学示例过度抽象。
- 共享业务逻辑优先使用纯 Dart。
- 平台差异集中在适配层，不在业务代码中散布平台判断。
- 新增依赖前核对维护状态、iOS／Android 支持情况和 Flutter-OH 适配情况。
- 未验证的鸿蒙插件能力不得进入教程正文，更不能写成已支持。

## 验证规范

- 文档改动至少检查 Markdown 结构、内部链接和命令拼写。
- Dart 示例落地后至少运行 `flutter analyze` 和 `flutter test`。
- iOS 示例逐章验证；Android 只做官方一手来源核验和差异说明，不做运行、构建或截图验证；鸿蒙按 `ROADMAP.md` 中的阶段门验证。
- 只有已实现并通过对应验证的内容才能在 `ROADMAP.md` 标记为已完成。

## 目录约定

```text
docs/chapters/       正文章节
assets/diagrams/     原创结构图和流程图
assets/screenshots/  三端运行截图
examples/            单概念最小示例
scripts/             文档和代码验证脚本
```

贯穿课程的完整应用在开始编码时放入 `app/`。在环境、版本和项目创建步骤确定前，不提前生成应用骨架。
